{
  "name": "blin",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "gateway",
    "services/*",
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present && npm run build:lambda --workspace=gateway"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "tsx": "^4.0.0",
    "@types/node": "^20.0.0"
  },
  "type": "module"
}